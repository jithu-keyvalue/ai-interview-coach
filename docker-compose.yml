version: "3.8"
services:
  db:
    image: postgres
    container_name: interview-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: # 📝 TODO: Connect this to the right env variable
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
